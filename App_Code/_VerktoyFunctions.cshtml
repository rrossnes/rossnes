@functions{
    
public static String PersonNummer(String dag, String mnd, String aar, String kjonn){
    //todo
    int individ;
    String result = "";
    int p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11;

    //dato posisjoner
    p1 = dag.Substring(0,1).AsInt();
    p2 = dag.Substring(1,1).AsInt();
    p3 = mnd.Substring(0,1).AsInt();
    p4 = mnd.Substring(1,1).AsInt();
    p5 = aar.Substring(2,1).AsInt();
    p6 = aar.Substring(3,1).AsInt();

    //individ posisjoner - generere individnummer mellom 0 og 999
    var random = new Random(1000);
    p7 = random.ToString().AsInt();
    random.Next();
    p8 = random.ToString().AsInt();
    random.Next();
    p9 = random.ToString().AsInt();

    //beregning av kontrollsiffer

    p10 = (11 - ((3 * p1) + (7 * p2) + (6 * p3) + (1 * p4) + (8 * p5) + (9 * p6) + (4 * p7) + (5 * p8) + (2 * p9))%11);
    p11 = (11 - ((5 * p1) + (4 * p2) + (3 * p3) + (2 * p4) + (7 * p5) + (6 * p6) + (5 * p7) + (4 * p8) + (3 * p9) + (2 * p10))%11);

    //verifisering av personnummer

    //dummy return
    //String result = String.Concat(dag,mnd,aar,kjonn);
    result = String.Concat(p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11);
    return result;
    
}


}